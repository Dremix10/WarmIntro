// Production landing page — delegates to the animated v2 landing the cofounder
// built at design-lab/landing-v2. Re-export keeps a single source of truth for
// the landing markup + animations.
export { default } from "./design-lab/landing-v2/page";
